{
  "name": "aido-lab-tests",
  "version": "1.0.0",
  "description": "Playwright tests for AIDO Lab platform",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report",
    "install": "playwright install"
  },
  "keywords": ["playwright", "testing", "e2e", "aido-lab"],
  "author": "AIDO Lab Team",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
